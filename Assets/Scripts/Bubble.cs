using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bubble : MonoBehaviour
{
    public string BubbleColor { get; set; } // Assign a color string like "Red", "Blue", etc.
    public int BubblePositionX { get; set; }
    public int BubblePositionY { get; set;}

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
